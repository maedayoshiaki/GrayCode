[project]
name = "graycode"
version = "0.1.0"
description = "Projector–camera gray-code geometric calibration and image warping"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.3.4",
    "opencv-contrib-python>=4.11.0.86",
    "scipy>=1.16.3",
    "torch>=2.9.1",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/graycode"]

[dependency-groups]
dev = [
    "pytest>=8.0",
    # 評価図 (graycode.evaluation.viz) のヒストグラム/ヒートマップ/誤差ベクトル場用。
    # 任意依存: 不在でも生の誤差配列(.npy)と統計サマリは常に出力される。
    "matplotlib>=3.8",
]
